
name: Basic GitHub Action Demo

on:
  push:
    branches: [main]
  pull_request:

jobs:
  demo-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Print branch name
        run: echo "Branch: ${GITHUB_REF_NAME}"

      - name: List files
        run: ls -la

      - name: Print Git status
        run: |
